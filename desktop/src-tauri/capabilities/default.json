{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. The sidecar is spawned from Rust, which bypasses the JS ACL.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
