[package]
name = "sail-desktop"
version = "0.2.0"
description = "SAIL — Sovereign AI Inference Layer desktop client"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"

[profile.release]
strip = true
lto = true
